import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.microsakurastudio.app',
  appName: 'Micro Sakura Studio',
  webDir: 'dist'
};

export default config;
